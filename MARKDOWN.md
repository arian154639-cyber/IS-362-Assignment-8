I chose to begin with pd.read_csv because I knew from other projects that read_csv works on many files that use commas for delimiters. I used header=None to ensure the first row is read instead of treated as a header row. I kept edibile status, odor, and bruises, then renamed them to those names. I then used map (which I had encountered during my research a few weeks ago for another assignment) to transform the data. My choices were between map and replace. For this specific scenario I went with map because both could have worked so I just chose one (replace could have worked with column specifying according to my research). For my plots, I followed the most basic way to create a histplot, with some extras like ylabel specifying. I chose a histplot because according to my research, histplots show distribution. The only plots I'm not sure of are the scatterplots, because those go by category instead of mushrooms. I think I did those wrong so I can't comment if odor or bruises impact edible status. 

Additional Note: I would have spent more time on the scatterplots but I am feeling unwell at the time of writing this which is why I didn't try to see what is wrong with my scatterplots.

Additional Note: I remembered the feedback from a few weeks ago to have iterative outputs, hence separating each plot into its own script. I learned how to import the main script to the plot scripts in another course.